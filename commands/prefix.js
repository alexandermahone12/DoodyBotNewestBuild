module.exports = {

    name: 'prefix',
    aliases: ['changeme', 'removeslash'],
    description: "creates an invite",
    execute(message, args, cmd, client, Discord, prefix) {
            if(args[0] === '!'){
                prefix === '!'
            }
    }
 
}